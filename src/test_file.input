fn main() {
  let x: u8 = 5;
  let y: u8 = 10;
  fn sum(a: u8, b: u8) -> u8{
    fn other_sum(x: u8, y: u8) -> u8 {
        return x + y;
    }
    return other_sum(a, b);
  }
  let z = other_sum(x, y);
  return z;
}